{
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0",
    "rollup-plugin-buble": "^0.15.0"
  },
  "scripts": {
    "build": "babel src -d output"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
